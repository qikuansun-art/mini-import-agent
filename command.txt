Fix size_spec validation integration.

Problem:
expand_size_spec_rows adds row["length"], row["width"], row["thickness"],
but DataValidator only validates fields that exist in mappings.
So length and width are still reported missing.

Requirements:

1. In workflow/executor.py:
After expanding size_spec, ensure state.mappings contains virtual FieldMappingItem for:
- source_header="length", target_field="length", confidence=1.0, source="size_parser"
- source_header="width", target_field="width", confidence=1.0, source="size_parser"
- source_header="thickness", target_field="thickness", confidence=1.0, source="size_parser"

2. Only add a virtual mapping if that target_field is not already mapped.

3. reason should be:
"Derived from size_spec"

4. Do not modify README files.

After changes, explain what changed.